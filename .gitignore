node_modules/

# registry.json is a cache — rebuilt by core/scripts/registry.mjs --build, in any state dir
**/.sdlc/registry.json

# the RentPoint test project: a workspace, regenerated by each phase's dod step.
# The committed proof of a phase is docs/dod/phase-<n>.md, not this folder (brief §7).
test/
