# backlog — things noticed during a phase that are not that phase's DoD

Constitution rule 9: nothing here is picked up before phase 6's DoD is green. One line each: where it was seen · what · the fix if known.

- **2026-09-06 · phase 6 build · qa:cases cannot supply a path id** — a step is not runnable when `API.path` holds `{id}`, so every confirm/edit/delete endpoint stays out of reach even now that the resource segment is right: TC-rental-005/006 (`/api/bookings/{id}/confirm`) and eight more. `TC.data` already carries the golden row; it could carry the id a prior step created, or `run` could thread the previous step's response into the next one's path.
- **2026-09-06 · phase 6 build · qa:cases false pass on an exception flow** — a non-main flow expects `4xx`, and an endpoint answers 400 to a body it cannot parse, so TC-rental-004 (BR-rental-006@v1 maintenance) passed on `HTTP_STATUS=400` with an empty request and never exercised the rule. `expect` for an exception flow should name the status and the `title`/`rule` the refusal must carry, not any 4xx. Same root as the http-body line above.
- ~~**2026-09-06 · backlog E · dev can only plan a use case, so more than half the design is unbuildable**~~ — **done 2026-09-06**: `dev:plan` now mints one TSK per (origin, generatorKey) for the screens no use case produced. `plan rental` = 21 tasks (9 kept byte-for-byte, 12 new), build order `baseline → master → usecase → nfr`, and the callsheet opens the build at `/dev:task TSK-010 --start` (login) instead of the first use case. See `plugins/dev/SPEC.md` amended 2026-09-06.
- **2026-09-06 · backlog E · the login screens declare no endpoint, so the auth slice has no route to build** — TSK-010 covers UI-rental-017 + UI-rental-025 and 3 ACL rows, and its slice prints no API at all: `sign-in` carries `api: null` in both screens, and `design:api` generates from a screen's *write* actions, which a sign-in is not. The action itself is declared, so it is not a Class B question — what is missing is the route. Either `design:api rental --records` declares the sign-in/token endpoints, or `/dev:revise UI-rental-017 --route "POST /api/auth/login"` writes the GAP and the `/change:open` line for the owner. Until one of them runs, an auth slice would be inventing its own API.
- **2026-09-06 · backlog E · `dev:handoff rental` is now 20 tasks away, not 8** — with 21 tasks planned and 2 verified, the manifest qa reads cannot be written until the other 19 are built. That is the honest count and not a bug: it was only ever 8 because 12 of them did not exist. It does mean `qa:cases` keeps working off `verifiedUseCases()` (which ignores tasks with `usecase: null`) and no test case will ever cover a baseline screen until qa learns to read a screen task.
- **2026-09-06 · backlog B · `change:impact` treats `refs` as a dependency** — `dependents()` walks every edge, and core writes a `refs` edge for any known id that appears anywhere in a record. So CR-004 (touching one scenario) pulled in TSK-001 and its 17 IMPs because the dev slice printed SCN-rental-001, and DEF-rental-001/003 because their reproduce text names it: 22 affected and 19 `G-change-005` freeze warns for a change that adds a scenario. A dependency is a named edge (`covers` `implements` `verifies` `displays` `serves`); `refs` is "mentions". Fixing the walk changes what every past impact would have said, so `docs/dod/phase-3.md` has to be re-verified with it — that is the whole job, not a flag.
- **2026-09-06 · backlog B · `qa:finding --kind` defaults to `rule`** — a finding routed to design opens its CR with `kind: rule`, which `impact` reads as "re-versions the BR and everything that enforces it" → lane full. CR-004's real request was "add the boundary scenario nobody wrote", which is not a rule change at all; it took a `change:open --amend` to correct. Either the default becomes `other` (the kind with no discriminator, where the owner names the lane) or `finding` asks for `--kind` when the routing is design.
- **2026-09-06 · backlog A · one `API.sample` cannot serve every scenario of an endpoint** — the sample is one declared call, so it fits one golden row and one flow. On RentPoint API-019's sample fits GD-rental-001 "3 วัน" (เครื่องตบดิน, 800/วัน) and nothing else: SCN-rental-002 (maintenance) needs a different equipment id, SCN-rental-018 needs the 1,250/วัน one, and SCN-rental-019/020 are golden rows of CALC-002 whose input is a fee, not dates — those two are unit-test territory (brief §5.6: unit = dev, E2E = qa) and may never belong to an http step at all. Next shape: a list of samples each naming the golden row or flow it serves, picked by `match.label`/`SCN.flow`.
- **2026-09-06 · backlog A · a sample gap is routed to dev** — `cases` writes `routing: dev` for every case it cannot make runnable, as the SPEC says ("a DEF routing: dev per TC it could not make runnable"). But "API-019.sample does not cover this scenario" is a declaration nobody wrote, not code that misbehaves: DEF-rental-006..009 point dev at work that belongs to whoever writes `--records`. Either the SPEC gains a second routing for a declaration gap, or `cases` reads the gap text and routes design when the missing record is a design one.
- **2026-09-06 · session · the marketplace is never installed, so no `/req: /design: /change: /mock: /dev: /qa:` command exists** — `/dev:task TSK-010 --start` answers `Unknown command`. `.claude-plugin/marketplace.json` lists all 7 plugins and every `plugins/*/commands/*.md` is on disk, but `specops` is in neither `~/.claude/settings.json → extraKnownMarketplaces` nor `~/.claude/plugins/known_marketplaces.json`: it has never been added on this machine. Only `/phase` works, and only because it is a project command in `.claude/commands/`. Every DoD in `docs/dod/` was proven by running `node plugins/<p>/scripts/<cmd>.mjs` directly — which means the slash-command layer each SPEC's Cold Start Test assumes has never once been executed. Adding it (`/plugin marketplace add D:ProjectClaudeSpecOps` then `/plugin install core@specops` … per plugin) is a one-time owner action, and the first thing to check after it is whether the `commands/*.md` argument-hints and `allowed-tools: Bash` actually run the scripts they name.
- **2026-09-06 · backlog E · nothing ever writes `GAP.cr`, so a GAP stays open after its CR exists** — `dev:revise` writes the GAP with `cr: null` and the exact `/change:open` line; the owner runs it, CR-005 is created — and no script links them. Grep says no file in `dev/` or `change/` ever assigns `gap.cr`, and W1 forbids change from writing `dev/gaps.json` anyway, so `G-dev-007` ("a GAP with `cr: null` and no answer") warns forever on a question that has already been asked. Two shapes: `dev:revise --gap GAP-002 --cr CR-005` writes it back (owner runs one more command), or `G-dev-007` derives the link — a GAP is answered when an open CR touches `gap.about` and names the same `asks` — and the field stops being state nobody maintains.
- **2026-09-06 · backlog E · the callsheet offers a task an open CR has frozen** — after CR-005 froze UI-rental-017, `core:next` still prints `10. /dev:task TSK-010 --start` because dev's NEXT rule picks the first `draft` task by build order and never asks `isFrozen`. Typing it is safe — `--start` refuses with `CANNOT START … frozen by CR-005` and names the way out — but Cold Start 0 should not hand somebody a command that cannot run. One line in the rule (skip a task whose `usecase`/`screens[]` are frozen) fixes it; the reason it is here and not fixed is that the dev selftest has no draft task left by the time its CR is opened, so the row cannot go red without restructuring the fixture — and a check that cannot go red is not a check.
