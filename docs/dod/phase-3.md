# DoD — phase 3 (change)

Run 2026-09-05 on Windows 11 · Node v22.19.0 · state dir `test/rentpoint/.sdlc` on top of the phase-2 design records (28 screens, 49 ACL rows, 32 endpoints, everything still `draft`). The three requests are testproject §6 word for word; the ids they touch were looked up with `/core:query`, never by opening a folder.
Every output cell is pasted from the command in its row. Rows that did not produce real output do not pass, by rule.

| DoD item | result | proof command | output |
|---|---|---|---|
| change plugs in: root in project.json, 6 gates appended, nothing else written | PASS | `node plugins/change/scripts/init.mjs --state-dir test/rentpoint/.sdlc` | INIT change · plugins.change.root written  gates added 6 of 6 |
| CR-001 opened from the client's words; touches the booking-list screen, names the field | PASS | `node plugins/change/scripts/open.mjs --state-dir test/rentpoint/.sdlc --kind display --source client --title "แสดงเบอร์โทรลูกค้าบนหน้ารายการจอง" --request "แสดงเบอร์โทรลูกค้าบนหน้ารายการจอง (backoffice)" --touches UI-rental-003 --fields ENT-006.phone` | OPEN CR-001  display · client · module rental · app backoffice · touches: UI-rental-003 · fields: ENT-006.phone · written: …/change/open/CR-001.json |
| CR-001 → lane **ui**, decided by the discriminator with three readable reasons, not by the request | PASS | `node plugins/change/scripts/impact.mjs CR-001 --state-dir test/rentpoint/.sdlc` | lane ui (decided by discriminator) · yes · ENT-006.phone already exists on Customer · yes · UI-rental-003 is already viewable by ROLE-001, ROLE-002 (ACL-014, ACL-035) · yes · no CALC is touched or affected |
| CR-001 impact = the booking-list UI + its ACL/API dependents, nothing else — no BR, no UC | PASS | `node plugins/change/scripts/impact.mjs CR-001 --state-dir test/rentpoint/.sdlc` · `grep -cE '"(BR\|UC)-' test/rentpoint/.sdlc/change/open/CR-001.json` | affected 4:  UI 1 · API 1 · ACL 2 — UI-rental-003 touched · API-020 serves <- UI-rental-003 · ACL-014 refs <- UI-rental-003 · ACL-035 refs <- UI-rental-003 · grep: 0 |
| CR-001 to create = MCK 1 · TSK revise 1 · TC 1 | PASS | `node plugins/change/scripts/impact.mjs CR-001 --state-dir test/rentpoint/.sdlc` | MCK 1 /mock:wireframe next would be MCK-rental-001 · TSK 1 /dev:revise next would be TSK-001 · TC 1 /qa:cases next would be TC-rental-001 |
| CR-002 (screen, customer) → lane **full**, to create UC 1 · UI 1 · API 1 · ACL 1 · SCN 1, no BR | PASS | `node plugins/change/scripts/open.mjs … --kind screen --source client --app customer --request "ลูกค้าขอหน้า \"ประวัติการเช่าของฉัน\" (read-only)"` · `node plugins/change/scripts/impact.mjs CR-002 --state-dir test/rentpoint/.sdlc` | OPEN CR-002  screen · client · module rental · app customer · touches: (nothing yet) → lane full (decided by kind) · a screen the client has never seen is a capability no artifact declares · affected 0 · UC 1 (UC-rental-010) · UI 1 (UI-rental-029) · API 1 (API-033) · ACL 1 (ACL-050) · SCN 1 (SCN-rental-018) |
| CR-003 (report) → lane **full** because of the totals; to create CALC-rental-004 + GD · RPT 1 · UI 1 · SCN 1 · TC 1 | PASS | `node plugins/change/scripts/open.mjs … --kind report --source client --app backoffice --request "รายงานรายได้รายเดือนแยกหมวดอุปกรณ์ พร้อมยอดรวม"` · `node plugins/change/scripts/impact.mjs CR-003 --state-dir test/rentpoint/.sdlc` | lane full (decided by kind) · a report carries totals, and a total is a number nobody has signed yet · CALC 1 /req:calc next would be CALC-rental-004@v1 · GD 1 (GD-rental-004) · RPT 1 (RPT-rental-001) · UI 1 (UI-rental-029) · SCN 1 (SCN-rental-018) · TC 1 (TC-rental-001) |
| apply CR-003 names `/req:ask` first — the report goes back to req before anything is drawn | PASS | `node plugins/change/scripts/apply.mjs CR-003 --state-dir test/rentpoint/.sdlc` | 1. /req:ask rental --cr CR-003 · 2. /req:calc rental --cr CR-003 · 3. /req:golden rental · 4. /design:screens rental · 5. /design:scenario rental · 6. /mock:wireframe backoffice · 7. /dev:task --cr CR-003 · 8. /qa:cases rental |
| apply CR-001 = the ui lane in order, and it says what it froze | PASS | `node plugins/change/scripts/apply.mjs CR-001 --state-dir test/rentpoint/.sdlc` | APPLY CR-001  lane ui · 1. /design:screens rental · 2. /mock:wireframe backoffice · 3. /dev:revise --cr CR-001 · 4. /qa:cases rental · frozen until CR-001 closes — isFrozen() answers true for 4: UI-rental-003, API-020, ACL-014, ACL-035 |
| after apply CR-001, `/design:screens rental` exits 1 naming the touched UI | PASS | `node plugins/design/scripts/screens.mjs rental --state-dir test/rentpoint/.sdlc; echo EXIT=$?` | SCREENS rental  created 0  already there 28  skipped 1 · frozen by an open CR — regenerate after the change closes: UI-rental-003 · EXIT=1 |
| apply wrote nothing outside change/ — the frozen UI is still design's, still `draft`, one `touches` edge | PASS | `node plugins/core/scripts/query.mjs UI-rental-003 --state-dir test/rentpoint/.sdlc --no-body --depth 1` · `cat test/rentpoint/.sdlc/trace.change.json` | ## UI-rental-003  [draft]  owner=design  file=design/rental/screens/backoffice-usecase.json · <- pointed at by: CR-001 [approved] (touches) · API-020 (serves) · ACL-014 (refs) · ACL-035 (refs) · OPEN CR touching: CR-001 · trace.change.json edges: {"from":"CR-001","rel":"touches","to":"UI-rental-003"} |
| change mints no id it does not own: the CALC preview is printed, never written | PASS | `grep -cE '"CALC-rental-004' test/rentpoint/.sdlc/change/open/CR-003.json` | 0 (toCreate on disk is `{kind, count, by}` only) |
| `isFrozen(id, ctx)` answers from disk for mock/dev/qa — frozen ids name the CR and the lane, the rest say no | PASS | `node --input-type=module -e "import { isFrozen } from './plugins/change/scripts/checks.mjs'; …"` | UI-rental-003 {"frozen":true,"by":"CR-001","lane":"ui"} · API-020 {"frozen":true,"by":"CR-001","lane":"ui"} · ACL-014 {"frozen":true,…} · UI-rental-004 {"frozen":false} · BR-rental-001@v1 {"frozen":false} |
| close refuses (exit 1, a finding) while anything it touched is below approved; nothing moved | PASS | `node plugins/change/scripts/close.mjs CR-001 --state-dir test/rentpoint/.sdlc --sign STK-001 --evidence docs/testproject-rentpoint.md; echo EXIT=$?` | CANNOT CLOSE CR-001 — 4 artifact(s) it touched are not back at approved: UI-rental-003 [draft] · API-020 [draft] · ACL-014 [draft] · ACL-035 [draft] · EXIT=1 · change/open still holds CR-001 CR-002 CR-003, change/closed absent |
| gates.mjs exit 0 on test/rentpoint with three CRs open — 41 gates, warns only | PASS | `node plugins/core/scripts/registry.mjs --build --state-dir test/rentpoint/.sdlc && node plugins/core/scripts/gates.mjs --state-dir test/rentpoint/.sdlc; echo EXIT=$?` | BUILT registry.json  artifacts=249 edges=546 files=78 · WARN  G-req-010  req questions: 2 open: DQ-rental-002, DQ-rental-001 · gates=41  error=0  warn=1  limit=0 · EXIT=0 |
| no artifact file > 300 lines | PASS | `find test/rentpoint/.sdlc -type f -not -name registry.json -not -name gates.json -not -path '*/export/*' \| xargs wc -l \| sort -n \| tail -2` | largest artifact file: req/rental/requirements.json 201 lines · 87 files (gates.json 293 — see note) |
| Cold Start 3: "ตอนนี้อะไรถูกแช่แข็งอยู่ เพราะ CR ไหน และเป็น lane อะไร" — from /core:query only | PASS | `node plugins/core/scripts/query.mjs CR-001 --state-dir test/rentpoint/.sdlc --depth 1` | ## CR-001  [approved]  owner=change  file=change/open/CR-001.json · body: {…"touches":["UI-rental-003"],"lane":"ui","impact":{…"affected":[UI-rental-003, API-020, ACL-014, ACL-035],"counts":{"UI":1,"API":1,"ACL":2}…},"applied":{…"commands":["/design:screens rental","/mock:wireframe backoffice","/dev:revise --cr CR-001","/qa:cases rental"]}…} |
| core:next puts the open CRs first and, per CR, names the next command from the state | PASS | `node plugins/core/scripts/next.mjs --state-dir test/rentpoint/.sdlc --all` | 1. resolve open CR: CR-001, CR-002, CR-003 — /change:impact then /change:apply · … · 7. /change:apply CR-002 (lane full, walked but not handed over) · 8. /design:screens rental (CR-001 (lane ui) is applied; 4 artifact(s) still below approved … then /mock:wireframe backoffice · /dev:revise --cr CR-001 · /qa:cases rental) · 9. /req:ask rental --cr CR-003 (CR-003 creates rather than changes, so no status can say when it is done) |
| change selftest (fixtures clean/dirty + live commands + every refusal) · the other three unchanged | PASS | `for p in core req design change; do node plugins/$p/scripts/selftest.mjs \| tail -1; done` | selftest PASSED · req selftest PASSED (18) · design selftest PASSED (29) · change selftest PASSED (31) |

What the run changed in the plugin, and why:
- `checks.mjs` NEXT: a CR that touches nothing (CR-002, CR-003 — a new screen, a new report) has no artifact whose status can say when it is done, so the callsheet pointed at `/change:close` the moment it was applied. Now it points at the first command owed and says the owner closes it after the last one; the version with the fix is what row "core:next" pasted. `selftest` still 31/31.

Deliberate differences, all visible in the outputs rather than silent:
- PROMPTS §P3 ขั้น 2 says `apply: reopen เป็น draft`; the approved SPEC says apply writes nothing outside `change/` (W1). The SPEC was followed: the freeze is `isFrozen()` asked by every plugin, and the touched UI stays `draft` under design's ownership — row "apply wrote nothing outside change/". design's `screens` already refuses on `touches`; it switches to `isFrozen()` in phase 4 as the SPEC says.
- `G-change-005 change:frozen` prints nothing on RentPoint: it lists frozen artifacts at `approved` or beyond (SPEC wording) and everything here is still `draft`. The Cold Start answer therefore comes from `/core:query CR-001`, `/core:query UI-rental-003` ("OPEN CR touching: CR-001") and `/core:next` — the three places the SPEC names — plus `isFrozen()` for the plugins.
- The walk is one-way (dependents only). Brief §5.3 says "up/down"; the SPEC enumerates the edges that point *at* the touched ids, and the DoD row "no BR, no UC" is only satisfiable that way — walking down from UI-rental-003 reaches UC-rental-002 and from there every BR it enforces.
- CR-002 was walked but not applied, on purpose, so that `/core:next` shows the "lane, not applied → apply" rule firing.

Carried to phase 4, not fixed here:
- `gates.json` is 293 lines after 41 gates. mock's `init` will push it past 300. It is a core file (excluded from G-core-005 and never read whole by a command), but rule 4 is about files the AI reads. Options when phase 4 opens: shorter `rule` strings, or one line per gate — a decision for the owner, noted here rather than made in a DoD run.
- `/core:next` without `--all` caps at 3 actions and core's own rules take the first slots, so change's per-CR rows (7–9 above) are only visible with `--all`. Core's behaviour since phase 0; not changed by change.
