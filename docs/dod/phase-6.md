# DoD — phase 6 (qa)

Run 2 · 2026-09-06 (Fable) on Windows 11 · Node v22.19.0 · dotnet SDK 8.0.424 · Docker 28.5.1 / Compose v2.40.2 · state dir `test/rentpoint/.sdlc` · code `b2d5712` (the TSK-002 commit).
Run 1 (2026-09-06, Fable) passed 17 of 21 rows. Its four red rows were not fixed by editing what they measure: `/phase 6 build` run 2 (Opus, 2026-09-06) fixed core's callsheet contract (row 16 then), made `design:api` derive a URL segment from an id-language name and refuse to write an empty one, gave design an `--approve`/`--cr` so a CR touching a screen can close (CR-001, open since phase 3, is the first CR this project ever closed), built TSK-002 with the BR-rental-004@v1 gap §8 describes, and gave `qa:cases` a `--refresh`. Row 11 was reworded by the owner's decision (SPEC §Phase DoD + testproject §8, `amended: 2026-09-06`): the 1,000 symptom can only exist on code that breaks the phase-5 unit test, so what is proved is the root cause and the routing.
This run re-executes every command on the state the build left, not on a wiped one: all 17 TCs were rewritten by the fixed `cases --refresh`, RUN-003/DEF-004/DEF-005 by the fixed plugin; DEF-001/002/003 and RUN-001/002 are run-1 records written by code paths that did not change. A wipe would also have made row 8 depend on command order (see notes). Rows that need first-time behaviour run on a scratch copy of the state dir, as run 1 did for the frozen-scenario row.
Every output cell is pasted from the command in its row. A row without a real command output does not pass, by rule.

| DoD item | result | proof command | output |
|---|---|---|---|
| `cases rental` from nothing → one TC per SCN, asks nobody, `runnable`/`gaps` per TC, a DEF `routing: dev` for every handoff gap under a verified use case (scratch copy: `qa/`, `trace.qa.json`, `export/qa-rental.md` removed, CR-004 kept) | PASS | `cp -r test/rentpoint/.sdlc <scratch>/.sdlc && rm -rf <scratch>/.sdlc/qa <scratch>/.sdlc/trace.qa.json <scratch>/.sdlc/export/qa-rental.md && node plugins/qa/scripts/cases.mjs rental --state-dir <scratch>/.sdlc` | CASES rental created 16 refreshed 0 already there 0 blocked 1 findings raised 2 · system under test: CMP-api http://localhost:8080 · TC-rental-003 SCN-rental-002 2 steps yes · 14 × no, each with its gap: `API-020 path is "/api/bookings/{id}/confirm" — it needs an id no record supplies` · `API-021.request is null` · `UC-rental-003 has no verified task` … · findings routed to dev: DEF-rental-001 TC-rental-004 · DEF-rental-002 TC-rental-005 (the two cases of UC-rental-002, which TSK-002 verified) · EXIT=1 |
| a scenario an open CR froze gets no new TC — exit 1 naming the CR | PASS | the same command | not written: SCN-rental-001 จองอุปกรณ์ — main — frozen by CR-004 (lane null) · close the change first · EXIT=1 |
| on the real state, re-running creates nothing; `--refresh` rebuilds steps and gaps against the fixed endpoints and keeps `lastRun`; no DEF is raised twice for one case | PASS | `node plugins/qa/scripts/cases.mjs rental --state-dir test/rentpoint/.sdlc --refresh` | CASES rental created 0 refreshed 17 already there 0 blocked 0 findings raised 0 · TC-rental-003 SCN-rental-001 2 yes · TC-rental-004 SCN-rental-002 2 yes · 15 × no · EXIT=0 |
| `run rental` → a new RUN, `codeVersion` = the TSK-002 commit, healthcheck first and kept as evidence, one verdict per TC with the reason; exit 1 because a case failed | PASS | `node plugins/qa/scripts/run.mjs rental --state-dir test/rentpoint/.sdlc` · `git -C test/rentpoint rev-parse HEAD` | RUN RUN-004 scope rental 17 case(s) pass 0 fail 1 partial 1 blocked 15 · code b2d57126 · node v22.19.0 · healthcheck up · qa/evidence/RUN-004-healthcheck-pass.log · TC-rental-005 BLOCKED — not runnable: API-020 path is "/api/bookings/{id}/confirm" … · next: /qa:finding TC-rental-003 … · EXIT=1 · HEAD b2d57126fee159f07b56f2f38d9cd6075e26bbef |
| **at least one http step executed against the slice, its verdict from `expect`, an evidence file per executed step** (run 1's row 5) | PASS | the same run · `ls test/rentpoint/.sdlc/qa/evidence` | TC-rental-003 verdict **`fail`** · step 1 http fail qa/evidence/TC-rental-003-RUN-004-s1-fail.log · HTTP 400, expected 201 · days is not in the response · fee is not in the response · step 2 ui skip …-s2-skip.log · TC-rental-004 **PARTIAL** · step 1 http pass qa/evidence/TC-rental-004-RUN-004-s1-pass.log · step 2 ui skip · evidence/: RUN-00{1,2,3,4}-healthcheck-pass.log · TC-rental-003-RUN-00{3,4}-s1-fail.log · …-s2-skip.log · TC-rental-004-RUN-00{3,4}-s1-pass.log · …-s2-skip.log · TC-rental-003-hand-fee3000-deposit.log · TC-rental-006-hand-confirm-unpaid.log — two executed steps, both verdicts read off the bytes (see notes: the failed verdict is the body-shape gap, the passed one is a false pass) |
| a run is never overwritten: RUN-001..003 untouched, `lastRun` of the case moves | PASS | `ls test/rentpoint/.sdlc/qa/runs` · `git -C test/rentpoint status --porcelain -- .sdlc/qa/runs/RUN-001.json` · `sha1sum …/RUN-003.json` before and after · `node plugins/core/scripts/query.mjs TC-rental-003 --state-dir test/rentpoint/.sdlc --depth 0` | RUN-001.json RUN-002.json RUN-003.json RUN-004.json · RUN-001 clean against the commit · RUN-003 6816a1e3cffd (unchanged) · TC-rental-003 "lastRun":"RUN-004","lastVerdict":"fail" |
| `finding` refuses without a failing run or an `--evidence` file (exit 2) | PASS | `node plugins/qa/scripts/finding.mjs TC-rental-007 --state-dir test/rentpoint/.sdlc --routing dev --reproduce "ปล่อยของได้ทั้งที่ยังไม่ confirmed"` | ERROR TC-rental-007's last run did not fail (blocked) — run it first, or bring what was seen instead: --evidence <file> · EXIT=2 |
| **testproject §8 DEF-rental-001** (ยืนยันจองได้ทั้งที่ยังไม่ชำระมัดจำ) observed on this build, raised `routing: dev`, `cr: null`, no file under `change/` | PASS | live on b2d5712: `curl -X POST /api/bookings …` → `curl "/api/bookings?customerId=…"` → `curl -X POST /api/bookings/<id>/confirm -d '{"by":"staff:1"}'` · `query.mjs DEF-rental-004 --depth 0` · `grep -rl DEF-rental-004 test/rentpoint/.sdlc/change/ \| wc -l` | created 8f2c67c6-… · before: status=draft depositPaid=false · `{"id":"8f2c67c6-…","code":"BK-20260906-B5825C","status":"confirmed"}` HTTP_STATUS=200 · DEF-rental-004: "tc":"TC-rental-006" "routing":"dev" "cr":null "severity":"s1" "evidence":["qa/evidence/TC-rental-006-hand-confirm-unpaid.log"] · files under change/ naming it: 0 — raised in the build run from the same capture (the log carries code b2d57126); this run reproduces the symptom on the same commit |
| one DEF per TC per routing: the same case routed to dev again, and to design again, is `ALREADY` — nothing written | PASS | `node plugins/qa/scripts/finding.mjs TC-rental-006 --state-dir test/rentpoint/.sdlc --routing dev --reproduce "…" --evidence …/TC-rental-006-hand-confirm-unpaid.log` · same for `TC-rental-003 --routing design --evidence …/TC-rental-003-hand-fee3000-deposit.log` | ALREADY DEF-rental-004 TC-rental-006 is already routed to dev — one defect per case per routing, not one per run · EXIT=0 · ALREADY DEF-rental-003 TC-rental-003 is already routed to design under CR-004 · EXIT=0 |
| **testproject §8 DEF-rental-002** (amended: the root cause — the slice answers fee 3,000 → 900 as GD-rental-002 says; no SCN proves the boundary, AC-rental-004) raised from live evidence, `routing: design`, a CR `source: finding` naming the DEF written by the same command | PASS | live on b2d5712: `curl -X POST /api/bookings -d '{…"pickupDate":"2026-09-03","returnDate":"2026-09-07","equipmentIds":["4444…"]}'` · `query.mjs DEF-rental-003 --depth 0` · `query.mjs CR-004 --depth 0` · `cat test/rentpoint/.sdlc/qa/evidence/TC-rental-003-hand-fee3000-deposit.log` (run 1's capture, same value) | `{"code":"BK-20260906-7AD39C","status":"draft","days":5,"fee":3000.00,"deposit":900,"lines":[{"equipment":"ปั๊มน้ำ","days":5,"rate":600.00,…}]}` HTTP_STATUS=201 — 900, not 1,000, on this build too · DEF-rental-003: "scenario":"SCN-rental-001" "routing":"design" "cr":"CR-004" "evidence":["qa/evidence/TC-rental-003-hand-fee3000-deposit.log"] · CR-004: "status":"draft" "kind":"rule" "source":"finding" "touches":["SCN-rental-001"] "finding":"DEF-rental-003" "lane":null |
| `report rental` → `export/qa-rental.md`, every number counted from records and equal to the registry's | PASS | `node plugins/qa/scripts/report.mjs rental --state-dir test/rentpoint/.sdlc` · `node plugins/core/scripts/registry.mjs --build --state-dir test/rentpoint/.sdlc` · `wc -l …/export/qa-rental.md` | REPORT rental · REQ 12 → UC 9 → SCN 17 → TC 17 (runnable 2) · pass 0 · fail 1 · partial 1 · blocked 15 · never run 0 · runs 4 · latest RUN-004 on b2d57126 · open findings: DEF-rental-001 dev · DEF-rental-002 dev · DEF-rental-003 design · CR-004 · DEF-rental-004 dev · DEF-rental-005 dev · registry: artifacts=320 edges=1068 files=130 · REQ 12 · UC 9 · SCN 17 · TC 17 · RUN 4 · DEF 5 · CR 4 · the document: 167 lines |
| the report counts AC coverage per AC, so the boundary CR-004 is about is visible (run 1 hid it behind AC-rental-001's flow) | PASS | the same report · `grep -c AC-rental-004 test/rentpoint/.sdlc/export/qa-rental.md` | acceptance criteria no scenario covers (a design gap, not a test gap): AC-rental-002, AC-rental-003, **AC-rental-004**, AC-rental-010 · in the document: 2 |
| `gates.mjs` exit 0 on test/rentpoint at the end — 61 gates, warns and the LIMITs only: G-qa-001..005 silent, G-qa-006 a warn, G-qa-007/008 LIMITs | PASS | `node plugins/core/scripts/gates.mjs --state-dir test/rentpoint/.sdlc; echo EXIT=$?` | WARN G-req-010 · WARN G-mock-004 · WARN G-dev-007 (GAP-001) · LIMIT G-dev-008 · WARN G-qa-006 qa: 15 test case(s) cannot be executed … · LIMIT G-qa-007 qa:ui-runner … · LIMIT G-qa-008 qa:no-duplicate-unit … · **gates=61 error=0 warn=4 limit=3 · EXIT=0** |
| `core:next` reads the qa state (run 1's row 16, the TypeError): open dev DEF → `/qa:run <TC>` · DEF with a CR → `/change:impact CR-004` · exit 0 | PASS | `node plugins/core/scripts/next.mjs --state-dir test/rentpoint/.sdlc --all` | 6. /change:impact CR-004 · 12. /qa:run TC-rental-003 · 13. /qa:run TC-rental-004 · 14. /qa:run TC-rental-006 (DEF-rental-004 is a bug for dev; when it is fixed, only a green run closes it) · 15. /qa:run TC-rental-005 · 16. /change:impact CR-004 (DEF-rental-003 routed to design and opened CR-004) · EXIT=0 |
| no artifact file the AI reads > 300 lines (gates.json exempt by the phase-4 decision) · no qa script > 300 | PASS | `find test/rentpoint/.sdlc -type f \( -name '*.json' -o -name '*.md' \) -not -name registry.json -not -name gates.json -not -path '*/export/*' \| xargs wc -l \| sort -n \| tail -3` · `wc -l test/rentpoint/.sdlc/gates.json plugins/qa/scripts/*.mjs plugins/qa/commands/*.md \| awk '$1>300'` | 201 req/rental/requirements.json · **232 dev/impl/TSK-001.json** (largest) · 7154 total · over 300: gates.json 433 only (61 gates) |
| Cold Start 6: "REQ-rental-001 สร้างแล้ว ทดสอบแล้ว ผ่านไหม หลักฐานอยู่ไหน" — from `/core:query` only | PASS | `query.mjs REQ-rental-001 --no-body --depth 1` · `… UC-rental-001 …` · `… SCN-rental-001 …` · `… TC-rental-003 --depth 0` · `… RUN-004 --depth 0` (all `--state-dir test/rentpoint/.sdlc`) | REQ-rental-001 <- UC-rental-001 [draft] (satisfies) · UC-rental-001 <- TSK-001 [verified] (implements) · <- SCN-rental-001 (verifies) · SCN-rental-001 <- TC-rental-003 [approved] (covers) · <- CR-004 (touches) · OPEN CR touching: CR-004 · TC-rental-003 "runnable":true "lastRun":"RUN-004" "lastVerdict":"fail" · RUN-004 "codeVersion":"b2d57126…" results[TC-rental-003] "verdict":"fail" "evidence":"qa/evidence/TC-rental-003-RUN-004-s1-fail.log" — the answer: built (TSK-001 at ccacd5d), run four times, last run executed and **failed**, and the file that says why is named on the RUN record |
| `docker compose down -v` (CMP-api `run.down`) leaves nothing behind | PASS | `docker compose -f api/docker-compose.yml down -v` (in test/rentpoint) | Network rentpoint_default Removing · Volume rentpoint_pgdata Removed · Network rentpoint_default Removed · EXIT=0 |
| qa selftest (clean/dirty fixtures · `checkExpect` reads the bytes · a live server run · findings dev/design · frozen SCN · report · the callsheet on live state · 31 checks) · the other six green, core now 9 checks with the NEXT contract | PASS | `for p in core req design change mock dev qa; do node plugins/$p/scripts/selftest.mjs \| tail -1; done` | selftest PASSED · req selftest PASSED (18) · design selftest PASSED (29) · change selftest PASSED (31) · mock selftest PASSED (27) · dev selftest PASSED (35) · qa selftest PASSED (31) |
| plugin shape: 4 commands (≤ 6), every script ≤ 300 lines, `init` idempotent — `plugins.qa.root` written and the 8 G-qa gates present once; the gate count did not move across the ten qa commands of this run | PASS | `ls plugins/qa/commands` · the 300-line row · `node -e "…project.json…plugins.qa.root"` · `node -e "…gates.json…length"` before and after | cases.md finding.md report.md run.md · plugins.qa.root = D:\ProjectClaude\SpecOps\plugins\qa · gates total 61 · G-qa 8 (61 before the first command of this run, 61 after the last) |

**19 of 19 rows pass.** The phase-6 DoD is green: two http steps ran against the slice and their verdicts came off the bytes, §8's dev-routed defect exists on the build and is DEF-rental-004 with no CR, §8's design-routed root cause is DEF-rental-003 → CR-004 opened by the same command, the report counts what the registry counts, the callsheet answers, and every plugin's selftest is green. Constitution rule 9: phase 7+ is reserved; `docs/backlog.md` may now be picked up.

Read before trusting the green — all visible in the outputs, none smoothed over:
- **TC-rental-003 `fail` is not a product defect.** `cases` sends the golden row's `input` (`{rate,pickup,return}`) as the body of an endpoint that takes `{customerId,pickupDate,returnDate,equipmentIds}`; the slice answers 400 `no-equipment`. The owner chose (row 5) to fix `design:api` and not to have qa invent the field mapping; it stays in `docs/backlog.md`.
- **TC-rental-004 `pass` is a false pass.** Its flow is an exception flow, so `expect.http` is `4xx`; the endpoint answers 400 to an empty body before it ever reaches BR-rental-006@v1. The rule the case is about was not exercised. Backlogged; the DoD counts the row as "a verdict from `expect`", which it is — and says here what that verdict is worth.
- **Row 8 depends on command order.** `cases` raises a dev DEF for every unrunnable case under a verified use case, and `finding` allows one dev DEF per case. On the real state DEF-rental-004 (the symptom) was raised in the build run *before* `cases --refresh`, so `cases` skipped TC-rental-006 and the symptom holds the slot. Run from nothing (row 1's scratch copy), `cases` puts the handoff-gap DEF on that case first and a later `finding` of the symptom is `ALREADY`. Two real defects on one case cannot both be recorded. Backlogged.
- **DEF-rental-001 and DEF-rental-002 are stale and open.** Raised in run 1 by `cases` for TC-rental-003/004's `/api//{id}` gap; that gap is gone and both cases now run, but a DEF closes only when its case passes, and one fails on the body shape while the other is `partial`. No command retires a `cases`-raised DEF whose gap was fixed upstream. Backlogged.
- **Ids are not §8's labels, on purpose.** §8 now says so under its table. The dev-routed symptom is DEF-rental-004; the design-routed root cause is DEF-rental-003 → CR-004.
- **The build-run commit `b2d5712` swept the run-1 qa state into git** (`git add -A` by the session for TSK-002; `--close` only checks outside `.sdlc/`). The run-2 qa records (TC refresh, RUN-003/004, DEF-004/005, evidence) are uncommitted. The owner commits.
- **`run rental` exits 1** when any case fails — the selftest expects it; the row above is green because the RUN was written and the verdict is right, not because the exit code is 0.

Carried, not fixed here:
- `gates.json` 433 lines / 61 gates — the phase-4 decision stands.
- `dev/impl/TSK-001.json` 232 lines — backlog (phase 5).
- A DEF raised from `--evidence` with no failing run is `source: "run"`, `run: null` (DEF-rental-003, DEF-rental-004) — backlog.
- design's `screens` reads open CRs itself (`openCrRecords`) rather than change's `isFrozen()`; mock, dev and qa use `isFrozen()`.
- RentPoint's api has no auth and takes `by` from the request body — backlog; the slice is a fixture.
